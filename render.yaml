services:
  - type: web
    name: mrsmovies
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:web_app
    healthCheckPath: /api/movies
    autoDeployTrigger: commit